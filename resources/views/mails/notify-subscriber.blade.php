<!DOCTYPE html>

<html>
    <head>
        <title>{{__('notify.html_title')}}</title>
    </head>
    <body>
        <h1>{{ $mailData['title'] }}</h1>

        <p>{{ $mailData['body'] }}</p>
        <p>{{__('notify.thansk')}}</p>
    </body>
</html>